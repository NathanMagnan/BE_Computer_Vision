%% load the image
image_beach = rgb2gray(imread("SpainBeach.jpg"));

%% diplay the image
figure(1);
imshow(image_beach);

%% create a new image with pixels in the right range
s=size(image_beach);
image_q10 = zeros(s(1), s(2));

for i = 1:s(1)
    for j = 1:s'2
